# Production profile (use when deploying: set SPRING_PROFILES_ACTIVE=prod). Do not put secrets here — use env vars.
# Required for password reset emails:
#   APP_FRONTEND_BASE_URL   e.g. https://yourdomain.com (no trailing slash)
#   SPRING_MAIL_HOST        e.g. smtp.sendgrid.net or your SMTP host
#   SPRING_MAIL_PORT        587 (TLS)
#   SPRING_MAIL_USERNAME     SMTP auth username (often your from-address)
#   SPRING_MAIL_PASSWORD    SMTP auth password or API key
