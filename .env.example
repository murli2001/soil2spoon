# Backend API base URL (no trailing slash).
# With backend running on port 8080 (e.g. mvn spring-boot:run from backend/):
# VITE_API_URL=http://localhost:8080

# With backend running on port 8081 (e.g. mvn spring-boot:run from backend/):
VITE_API_URL=http://localhost:8081

# -----------------------------------------------------------------------------
# Backend (Spring Boot). Set these when running or deploying the backend.
# Copy to .env or set in your shell/deployment; the backend reads them at runtime.
# -----------------------------------------------------------------------------

# JWT signing secret (min 32 bytes / 256 bits for HS256). Required in production.
# Generate a strong value, e.g.: openssl rand -base64 32
JWT_SECRET=

# Google Geocoding API key. Optional: if set, shipping addresses are validated via Google (pincode, city, state).
# Get a key from Google Cloud Console with Geocoding API enabled. Leave empty for format-only validation.
GOOGLE_GEOCODING_API_KEY=
